# {{hookName}}

{{description}}

## API
```typescript
interface {{hookName}}Return {
  {{#each returnValues}}
  {{this.name}}: {{this.type}} // {{this.description}}
  {{/each}}
}

function {{hookName}}({{#each params}}{{this.name}}: {{this.type}}{{#unless @last}}, {{/unless}}{{/each}}): {{hookName}}Return
```

## Exemplo de Uso
```typescript
function Component() {
  const { {{#each returnValues}}{{this.name}}{{#unless @last}}, {{/unless}}{{/each}} } = {{hookName}}({{#each params}}{{this.name}}{{#unless @last}}, {{/unless}}{{/each}})

  return (
    // Implementação
  )
}
```

## Regras
- {{#each rules}}
- {{this.description}} {{/each}}

## Testes
```typescript
describe('{{hookName}}', () => {
  it('should {{testCase}}', () => {
    // Implementação do teste
  })
})
```

## Changelog
| Versão | Mudança |
|--------|---------|
| 1.0.0  | Implementação inicial |
